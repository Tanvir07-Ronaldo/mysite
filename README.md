# Tanvir Ronaldo — Personal Website

This repository contains a static front-end site built with Vite + React.

## Preview locally

1. Install Node.js (LTS) from https://nodejs.org/
2. Open a terminal in the project folder.
3. Install dependencies:
   ```bash
   npm install
